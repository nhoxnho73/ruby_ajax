<div class="container">
<h1>Movies</h1>

<div class="row">
  <table class="table">
    <tbody>
      <tr> 
        <td><strong>Name</strong></td>
        <td><strong>Director</strong></td>
        <td><strong>Star</strong></td>
        <td><strong>Genre</strong></td>
        <td style="text-align: center;"><strong>Image</strong></td>
        <td></td>
      </tr>
      <% @movies.each do |movie| %>
        <tr>
          <td><%= movie.name %></td>
          <td><%= movie.director %></td>
          <td><%= movie.star %></td>
          <td><%= movie.genre.name %></td>
          <td style="text-align: center;"><a href="<%=genre_path(movie.genre.id)%>"><%= image_tag(movie.image.url,class: 'media-object', style: "width: 60px; height: auto;") if movie.image.exists? %></a></td>
          <td><%= link_to "Show", movie_path(movie.id), class: "btn btn-success" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<div class="row">
  <div class="col-md-8"></div>
  <div class="col-md-4">
    <%= will_paginate @movies, renderer: BootstrapPagination::Rails %>
  </div>
</div>
<div class="row">
  <div class="col-md-10">
    <%= link_to "Download", dowloads_path, class: "btn btn-success" %> |
    <%= link_to "Download Zip", detail_zips_path, class: "btn btn-primary" %> |
    <%= link_to "New Movie", new_movie_path, class: "btn btn-warning" %> |
    <%= link_to "Download pdf", download_pdfs_path(format: "pdf"), class: "btn btn-danger" %>
  </div>
</div>
</div>
